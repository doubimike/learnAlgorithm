function repeat(arr, num) {
    var result = '';
    while (num > 0) {
        result += str;
        num--
    }
    return result;
}

function repeat(arr, num) {
    var array = [];
    for (var i = 0; i < num; i++) {
        array.push(str);
    }

    return array.join('');
}


function repeat(arr, num) {
    var array = [];
    for (var i = 1; i <= num;) {
        array[i++] = str;
    }

    return array.join('');
}


function repeat(arr, num) {
    return Array.apply(null, { length: num + 1 }).join(str);
}


function repe(arr, num) {
    // if
}
